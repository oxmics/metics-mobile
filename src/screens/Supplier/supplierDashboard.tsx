import { View } from "react-native";
import { Text } from "react-native-paper";

const SupplierDashboardScreen = () => {
    return (
        <View>
            <Text>Supplier Dashboard</Text>
        </View>
    );
}

export default SupplierDashboardScreen;